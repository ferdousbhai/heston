-- The site starts fresh with its own briefs; the imported Telegram posts were retired from
-- every reader in the deploy before this one, so nothing live reads the table.
DROP TABLE long_vol_channel_posts;
